CREATE OR REPLACE FUNCTION zerobyte.ZEROBYTE_TO_ARRAY_TIME(a ARRAY< STRUCT< _ ARRAY< STRUCT< _ STRUCT<
INT64 > > > > >)
AS (
  ARRAY(
    SELECT
      zerobyte.ZEROBYTE_TO_TIME(_)
    FROM
      UNNEST(a) WITH OFFSET AS o
    ORDER BY o)
);
